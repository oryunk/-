# MySQL Workbench — 여기만 보면 됨

`add_*.sql`, `migrations/` **실행 안 해도 됩니다.**

## 필수 (순서대로 2개)

| 순서 | 파일 | 설명 |
|------|------|------|
| 1 | [`01_필수_테이블전체.sql`](01_필수_테이블전체.sql) | DB + 테이블 29개 생성 |
| 2 | [`02_필수_트리거프로시저.sql`](02_필수_트리거프로시저.sql) | 가입 시 모의계좌, 매수 프로시저 |

Workbench: 파일 열기 → **번개(Execute)** → 다음 파일.

**주의:** `01`은 **빈 DB에 한 번만**. 두 번 돌리면 "테이블 이미 있음" 오류.

---

## 선택

| 파일 | 설명 |
|------|------|
| [`03_선택_종목데이터.sql`](03_선택_종목데이터.sql) | KOSPI 종목 시드 (시장 페이지용) |
| [`선택_시드/`](선택_시드/) | 커뮤니티·고객센터 데모 글 (**회원 1명 먼저**) |

### 시드 순서 (회원가입 1번 한 뒤)

1. `선택_시드/community_posts.sql`
2. `선택_시드/community_comments.sql`
3. `선택_시드/support_inquiries.sql`

---

## 안 쓰는 것 (무시)

- `SQL/add_*.sql`, `update.sql` — 예전 방식, `01`에 이미 포함
- `SQL/migrations/` — 예전 DB 패치용. **새로 깔 때는 불필요**

---

## 이 폴더 파일 수정 시

원본은 `SQL/schema.sql`, `SQL/user_account.sql` 등입니다.  
스키마 바꿀 때 원본 수정 후 이 폴더 파일도 같이 맞춰 주세요 (또는 다시 복사).
